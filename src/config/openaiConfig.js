// OpenAI model/config for frontend (no secrets)
export const OPENAI_MODEL = "gpt-4o";
export const USE_MOCK_MODE = true; // Set to false to use real API
export const OPENAI_CONFIG = {
  temperature: 0.7,
  maxTokens: 6000,
};
